# Copiá a .env (misma carpeta)

# URL del sitio de la liga (sin barra final)
SITE_URL=https://tudominio

# El mismo valor que pusiste en IMPORT_TOKEN en el .env del servidor
IMPORT_TOKEN=

# opcional
UDP_PORT=20777
